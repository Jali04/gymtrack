/* =============================================
   GYMTRACK — Workout History
   History is now embedded in the Training/Log page.
   Stubs keep backward-compat (e.g. doImport).
   ============================================= */

function renderHistory() { renderLog(); }
function deleteWorkout(id) { deleteLogWorkout(id); }
